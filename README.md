# dasbf

A public website.

## Structure

```
index.html       - Homepage
css/styles.css   - Stylesheet
assets/          - Images and other static assets
```

## Adding pages

Place new `.html` files at the root of the repository. Link them from the navigation in `index.html`.